class playerScore{
  constructor(){
    this.x=x;
    this.y=y;
    this.scoreSize = scoreSize;
    this.score = score;
    this.heartContainer = heartContainer;
    this.heartSizeX = heartSizeX;
    this.heartSizeY = heartSizeY;
    this.heartContainerSpacing = heartContainerSpacing;


  }
  create(){
    heartContainer
    let heartImage1 = image(heartImg, this.x, this.y, this.w, this.h);
    let heartImage2 = image(heartImg, this.x, this.y, this.w, this.h);
    let heartImage3 = image(heartImg, this.x, this.y, this.w, this.h);

  }
}
